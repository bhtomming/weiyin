{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="card">
        <div class="card-title">{{ brand.title }}</div>
            <div class="card-body">{{ brand.content | raw }}</div>
        </div>
    </div>
{% endblock %}